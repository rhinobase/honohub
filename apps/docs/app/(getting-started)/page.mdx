import { Guides } from "../../components/Guides";
import { Features } from "../../components/Features";
import { HeroPattern } from "../../components/HeroPattern";
import { Button, Text } from "@rafty/ui";
import { ArrowRightIcon } from "@heroicons/react/24/outline";
import Link from "next/link";

export const metadata = {
  description:
    "Create fast and scalable APIs effortlessly using your Drizzle Schemas, and deploy it on the edge with Hono.",
};

export const sections = [{ title: "Guides", id: "guides" }];

<HeroPattern />

# HonoHub

Create fast and scalable APIs effortlessly using your Drizzle Schemas, and deploy it on the edge with Hono. Built on top of [Hono](https://hono.dev/), [Drizzle](https://orm.drizzle.team/), [Zod](https://zod.dev/), and [Vite](https://vitejs.dev/). {{ className: 'lead' }}

<div className="not-prose mb-16 mt-6 flex gap-3">
  <Link href="/quickstart">
    <Button
      colorScheme="primary"
      className="rounded-full py-1"
      rightIcon={<ArrowRightIcon width={16} height={16} className="stroke-2" />}
    >
      Quickstart
    </Button>
  </Link>
</div>

<Features />

<Guides />
