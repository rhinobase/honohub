export const metadata = {
  title: "Project Structure",
  description:
    "Create fast and scalable APIs effortlessly using your Drizzle Schemas, and deploy it on the edge with Hono.",
};

# Project Structure

You can declare your `config` and `collections` in TypeScript either in a single schema.ts file, or you can spread them around — whichever you prefer, all the freedom!

<CodeGroup>

```txt {{ title: '1 File' }}
// Everything in 1 file

📦 <project root>
├ 📂 src
└ 📜 honohub.config.ts
```

```txt {{ title: 'Separate Files' }}
// Scattered across multiple files

📦 <project root>
├ 📂 src
├ 📂 collections
|  ├ 📜 todos.ts
|  └ 📜 messages.ts
├ 📂 plugins
|  ├ 📜 custom.ts
└ 📜 honohub.config.ts
```

</CodeGroup>

You can define `config`, `collections`, `actions` and `plugins`